
from widgets import *


def getButton(idx):
    return buttonWidget.widgets[idx]


def getLed(idx):
    return LEDWidget.widgets[idx]


def getCanvas(idx):
    return canvasWidget.widgets[idx]


def getCamera(idx):
    return cameraWidget.widgets[idx]


def getImageFrame(idx):
    return imageFrameWidget.widgets[idx]
